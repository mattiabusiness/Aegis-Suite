// ============================================================================
// AEGIS BEAUTY - CUSTOMER ROUTE GROUP ROOT LAYOUT
// File: apps/aegis-beauty/app/(customer)/layout.tsx
// Passthrough — actual layout is handled in [businessSlug]/layout.tsx
// ============================================================================

export default function CustomerRootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
